#include <stdio.h>
/*
 * Main function has no return value
 * Main is the entry point of this program
 *
 * Return is always set to 0(success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
